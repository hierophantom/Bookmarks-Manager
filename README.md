# Bookmark Manager — Minimal Scaffold

This workspace contains a minimal scaffold implementing the provided spec: a bookmarks explorer, tag storage, a universal search overlay bridge, and a new-tab override.

Load locally via `chrome://extensions` -> "Load unpacked" and point to this project folder.
